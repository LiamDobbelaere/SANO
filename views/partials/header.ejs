<header>
    <h1>SANO</h1>
    <ul>
        <% if (typeof(links) !== 'undefined') links.forEach(function(item) {%>
        <li><a href="#"><%=item%></a></li>
        <%});%>
    </ul>
</header>